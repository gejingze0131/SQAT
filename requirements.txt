# Training env. vLLM is deliberately NOT here — it pins its own torch and lives in the
# separate eval env built by scripts/setup_vllm_env.sh (requirements-vllm.txt).
torch
transformers
accelerate
peft
bitsandbytes
datasets
pyyaml
lm-eval
wandb
tqdm
