results/
outputs/
outputs
wandb/
__pycache__/
src/__pycache__/
scripts/__pycache__/
# PBS writes <jobname>.o<jobid> into the submit directory; keep the root clean — they are
# archived under logs/job_outputs/ (tracked, like the rest of logs/).
/*.o[0-9]*
/gptq_log_*.log
datasets

# Pinned third-party checkouts the LoTA-QAF baseline patches rather than imports; they are
# rebuilt by baseline/LoTA-QAF/sqat/setup_env.sh at the commits it names.
baseline/LoTA-QAF/vendor/
.triton/
